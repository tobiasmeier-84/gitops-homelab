# proxmox-vm module

Reusable module for provisioning a single VM from the Debian cloud image, with cloud-init for hostname/network/SSH-key injection. Parameterized so it's used for both the RKE2 nodes and the HAProxy VMs.
